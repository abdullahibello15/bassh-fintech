services:
  - type: web
    name: your-app-name
    env: static
    buildCommand: npm run build
    staticPublishPath: ./dist # or ./dist if you're using Vite
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
